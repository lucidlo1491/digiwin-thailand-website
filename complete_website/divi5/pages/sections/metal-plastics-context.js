/**
 * metal-plastics-context.js — Context Section (S2)
 *
 * AUTO-GENERATED by scaffold-builder.js — review and customize before pushing.
 * Source: metal-plastics.html line 435
 *
 * styles.css: 8 base, 0 hover, 0 pseudo, 0 hidden
 * Original classes: context-section, fade-in, context-inner
 */

const base = require('../../lib/templates/_base');

const P = 'context'; // CSS prefix — customize if needed

// ════════════════════════════════════════════════════════════════
// BLOCKS
// ════════════════════════════════════════════════════════════════
function blocks() {
  const html = `
    <div class="${P}-inner">
                <h2>Where Margins Are Made</h2>
                <p>In metal and plastics processing, <strong>material costs are 50-70% of your total cost</strong>. Every defective part is money in the scrap bin. Every second of cycle time drift is capacity you'll never recover. Small improvements in yield and efficiency <strong>translate directly to the bottom line</strong>—but you can't improve what you can't measure.</p>
            </div>`;

  return base.wrapInDiviSection('Context', html, 'Context: Content');
}

// ════════════════════════════════════════════════════════════════
// CSS
// ════════════════════════════════════════════════════════════════
function css() {
  return `
/* === CONTEXT (S2) === */
/* Divi 5 section boilerplate (font-smoothing, p padding, heading resets) */
/* Divi wrapper override */
.et_pb_section:has(.context-inner){padding:100px 0 !important;background:#fff !important}
.context-section{${base.fontSmoothingReset(P)}font-size:16px}
.context-section p{padding-bottom:0;line-height:1.6}
.et_pb_section .context-section h2{margin:0;padding:0;line-height:1.6}
.context-inner{max-width:900px;margin:0 auto;padding:0 24px;text-align:center}
.context-inner h2{font-family:'Noto Sans', sans-serif;font-size:36px !important;font-weight:700 !important;color:#000864 !important;margin-bottom:24px !important;line-height:1.6 !important}
.context-inner p{font-size:18px;color:#5b6b80;line-height:1.8}
.context-inner p strong{color:#000864}


${base.reducedMotion('*{animation:none !important;transition:none !important}')}
`.trim();
}

module.exports = { blocks, css };
